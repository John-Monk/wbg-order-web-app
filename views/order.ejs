<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order</title>
  </head>
  <body>
    <form id="orderForm">
        <input name="location" type="hidden" value="<%= location %>">
        <label for="name" >Name</label>
        <input name="name" id="name" type="name" type="text" placeholder="Name">
        <ul>
          <% for(let i = 0; i < items.length; i++){ %>
          <li>
            <h3><%= items[i].name %> - <span><%= items[i].price %></span></h3>
            <input name="<%= items[i].name %>" type="number" value="0" min="0">
            <label for="<%=items[i].name%>-notes">Notes</label>
          </li>
          <% } %>
        </ul>
        <label for="notes">order notes</label>
        <textarea id="notes" name="notes" type="text"></textarea>
        <button type="submit">Submit Order</button>
        </form> 
    <script src="/js/main.js"></script>
  </body>
</html>
